class GetPendingFriend {
    constructor(closefriend) {
        this.closefriend = closefriend;
    }

    async execute(dto) {
        const { userid } = dto;

        try {
            
        } catch (error) {
            throw error;
        }
    }
}

module.exports = GetPendingFriend;